<UserControl x:Class="XmlJsonConverter.singleJsonElement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:XmlJsonConverter"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal">
        <TextBlock Text="{Binding preparedToShowName}"/>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="   "/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <CheckBox  Name="toConvert" IsChecked="{Binding Path=isChecked, Mode=TwoWay}"></CheckBox>
        </StackPanel>
    </StackPanel>
</UserControl>
